name: CI2
on:
  workflow_dispatch:
    inputs:
      job:
        type: choice
        derunion: 'Job to run.'
        required: true
        options:
          - build
          - shareddev
          
defaults:
  - name: RUN-Defaults
    run: |
      echo DEFAULTS

jobs:      
  build:
    env:
      GIT_SUBMODULE_STRATEGY: recursive
    runs-on:
      - ubuntu-latest
    steps:  
      - name: RUN
        run: |
            echo a
            echo b
    

